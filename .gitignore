node_modules/
.next/
.env.local
.env.production
*.tsbuildinfo

# Production runtime artifacts written by scripts/start-production.sh.
.next-server.pid
logs/

# Internal security notes — keep the threat model out of the public mirror.
SECURITY_FOLLOWUPS.md

# Bulk TSV deposits — regenerated by scripts/dump-tsv.sh (cron nightly).
# Ship via deploy artifact or Zenodo, not git.
public/data/*.tsv
public/data/*.sha256
public/data/MANIFEST.sha256
public/data/.generated-at
